import Hero from "../Hero/Hero";
import Nav from "../Nav/Nav";

const Header = () => {
  const SearchButton = () => {};

  return (
    <header>
      <Nav />
      <Hero />
    </header>
  );
};

export default Header;
