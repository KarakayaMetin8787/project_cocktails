import { createContext } from "react";

export const SearchFetchContext = createContext([]);
export const GoBackButtonContext = createContext("");
export const CreateProductContext = createContext([]);
